# Contributing

Development setup: `uv sync --all-groups`, `uv run pytest -q`, and `uv run ruff check .`. Consumer installation does not require a clone. Do not weaken evidence, policy, or image-integrity fail-closed behavior.
