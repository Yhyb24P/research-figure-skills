# Legacy directories

`nature-figure/`, `scientific-schematic/`, `figure-preflight/`, and `shared/` are retained as
non-runtime compatibility/reference material for the v2.1 release line. The installed product
uses only `src/research_figures/` and its bundled package resources. New product behavior must
not be added to the legacy directories; their removal is scheduled for a future breaking release.
